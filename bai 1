#include <stdio.h>
int main()
{
	int h;
	printf("nhap h:");
	scanf_s("%d", &h);	
	for (int i = 2; i < h; i++)
	{
		if (h % i == 0)
		{
			printf("%d day khong phai so nguyen to", h);
			break;
		}
	}
		printf("%d day la so nguyen to", h);
}
