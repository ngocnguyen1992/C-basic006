#include <stdio.h>
int main()
{
	int n;
	int s = 0;
	printf("nhap n:");
	scanf_s("%d", &n);
	for (int i = 1; i <= n; i++)
	{
			if (i % 2 == 0 && n%i==0 && i<n)
			{
				s = s + i;
			}
	}	
	printf("tong cac uoc so chan cua n la %d ", s);
}
